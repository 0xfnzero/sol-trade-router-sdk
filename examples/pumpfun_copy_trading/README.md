# PumpFun Copy Trading (Router)

[中文](README_CN.md)

Low-latency gRPC copy → Route CPI. Warm before subscribe.

```bash
export PRIVATE_KEY=...
export RPC_URL=https://your-rpc.example
export GRPC_ENDPOINT=https://your-yellowstone.example
cargo run -p pumpfun_copy_trading
```

See [docs/LOW_LATENCY_BOTS.md](../../docs/LOW_LATENCY_BOTS.md).
